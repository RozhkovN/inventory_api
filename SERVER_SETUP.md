# 🚀 Настройка для сервера (Server Setup)

## Порты

- **Frontend**: http://srv1:3700
- **Backend API**: http://srv1:8700/docs
- **Database**: srv1:5437

## Запуск на сервере

```bash
cd ~/main/inventory_api

# Остановить старые контейнеры (если были)
docker compose down

# Пересобрать образы
docker compose build --no-cache

# Запустить
docker compose up -d

# Проверить логи
docker compose logs -f
```

## Проверка статуса

```bash
# Все контейнеры должны работать
docker compose ps

# Проверить БД
docker compose logs db | tail -20

# Проверить бэкенд
docker compose logs -f backend

# Проверить фронтенд
docker compose logs -f frontend
```

## Если БД не создалась правильно

```bash
# Полностью очистить
docker compose down -v

# Пересобрать
docker compose build --no-cache

# Запустить с логами
docker compose up
```

## Доступ с другой машины

Если развертываешь на сервере, открой:
- Frontend: `http://<server-ip>:3700`
- API Docs: `http://<server-ip>:8700/docs`

## Решение проблем

### ❌ Ошибка: "database inventory_db does not exist"
**Решение**: Контейнер БД работает, но еще не инициализировалась. Подожди 10-15 сек и проверь логи:
```bash
docker compose logs db
```

### ❌ Ошибка: "Port 5437 already in use"
**Решение**: Измени порт в `docker-compose.yml`:
```yaml
ports:
  - "5438:5432"  # используй другой порт
```

### ❌ Фронтенд слушает на 3000 вместо 3700
**Решение**: Проверь что контейнер пересобран:
```bash
docker compose build --no-cache frontend
docker compose restart frontend
```

## Скрипт заполнения данными

```bash
# Убедись что API работает
python3 populate_db.py
```

## Мониторинг

```bash
# Смотреть все логи в реальном времени
docker compose logs -f

# Только бэкенд
docker compose logs -f backend

# Только БД
docker compose logs -f db

# Только фронтенд  
docker compose logs -f frontend
```

---

**Дата развертывания**: 27 декабря 2025
**Конфигурация**: Production-ready на сервере srv1
