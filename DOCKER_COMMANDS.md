# 🔧 Команды для очистки и переустановки на сервере

## ⚠️ ПОЛНАЯ ОЧИСТКА (удалит ВСЁ включая БД!)

```bash
# На сервере выполни эту команду:
cd ~/main/inventory_api && bash cleanup.sh
```

Или вручную по шагам:

```bash
# Шаг 1: Перейди в папку
cd ~/main/inventory_api

# Шаг 2: Останови и удали контейнеры
docker compose down -v

# Шаг 3: Удали образы
docker rmi inventory_api-backend inventory_api-frontend

# Шаг 4: Очисти Docker систему
docker system prune -f

# Шаг 5: Пересобери образы
docker compose build --no-cache

# Шаг 6: Запусти приложение
docker compose up -d
```

## ✅ Проверка статуса

```bash
# Смотри логи всего приложения
docker compose logs -f

# Только бэкенда
docker compose logs -f backend

# Только БД
docker compose logs -f db

# Только фронтенда
docker compose logs -f frontend

# Статус контейнеров
docker compose ps
```

## 🌐 Доступ к приложению

- **Приложение**: http://80.253.19.93:3700
- **API Docs**: http://80.253.19.93:8700/docs
- **Database**: 80.253.19.93:5437

## 🔍 Если что-то не работает

```bash
# Проверить БД подключение
docker compose exec backend python -c "from app.database import engine; engine.connect(); print('✅ БД подключена!')"

# Перезагрузить только фронтенд
docker compose restart frontend

# Перезагрузить только бэкенд
docker compose restart backend

# Полный рестарт
docker compose restart
```

## 📝 Важно!

⚠️ **cleanup.sh удалит БД!** Используй его только если нужно полностью очистить приложение.

Если просто нужно перезагрузить контейнеры:
```bash
docker compose restart
```

Если просто нужно пересобрать образы:
```bash
docker compose build --no-cache
docker compose up -d
```

---

**Что изменилось:**
- ✅ `api.js` — фронтенд теперь понимает как локальную разработку так и сервер
- ✅ Автоматическое определение IP адреса сервера
- ✅ Поддержка переменных окружения и fallback значений
