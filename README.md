# 📊 Inventory Manager - Полнофункциональное веб-приложение

Полнофункциональное веб-приложение для управления складом и учёта продаж с поддержкой коэффициентов (НДС, скидки и т.д.).

**Статус**: ✅ Готово к использованию | **Версия**: 2.0 | **Дата**: 27 декабря 2025

## 🚀 Быстрый старт

### Требования
- Docker и Docker Compose

### Запуск за 30 секунд

```bash
# 1. Перейти в папку проекта
cd inventory_api

# 2. Запустить контейнеры
docker-compose up -d

# 3. Открыть приложение в браузере
# Frontend:  http://localhost:3000
# Backend:   http://localhost:8000
# API Docs:  http://localhost:8000/docs
```

## 🎯 Структура проекта

```
inventory_api/
├── backend/              ← FastAPI приложение
│   ├── app/             
│   │   ├── main.py
│   │   ├── database.py
│   │   ├── routes/       (API endpoints)
│   │   ├── models/       (БД модели)
│   │   ├── schemas/      (Валидация)
│   │   └── services/     (Логика)
│   ├── requirements.txt
│   └── Dockerfile
│
├── frontend/             ← React приложение
│   ├── src/
│   │   ├── App.js
│   │   ├── Warehouse.js  (Склад)
│   │   ├── Sales.js      (Продажи)
│   │   ├── History.js    (История)
│   │   ├── api.js        (API клиент)
│   │   └── *.css
│   ├── package.json
│   └── Dockerfile
│
├── docker-compose.yml    ← Запуск всех сервисов
└── .env                  ← Переменные окружения
```

## 📋 Функциональность

### 📦 Склад (Warehouse Tab)
- ✅ Поиск товаров по названию (в реальном времени)
- ✅ Добавление товаров с коэффициентом (НДС, скидки)
- ✅ Редактирование товаров (цена, кол-во, коэффициент)
- ✅ Удаление товаров (если кол-во = 0)
- ✅ Автоматический расчёт итоговой цены

### 💰 Продажи (Sales Tab)
- ✅ Создание продаж с несколькими товарами
- ✅ Коэффициент для каждой позиции отдельно
- ✅ Автоматический расчёт суммы, себестоимости, прибыли
- ✅ Изменение статуса оплаты (Оплачено / Не оплачено / Частично)
- ✅ Отмена продажи (товары возвращаются на склад)

### 📊 История (History Tab)
- ✅ Полная история всех операций со складом
- ✅ Фильтрация по товарам, типам, датам
- ✅ Отслеживание всех изменений параметров

## 🖼️ Интерфейс

### Компоненты
- 📊 Навигационная панель с вкладками
- 📋 Интерактивные таблицы
- 🔍 Поиск в реальном времени
- 📝 Модальные окна для форм
- 📈 Карточки для отображения данных
- 🎨 Современный дизайн с адаптацией под мобильные

## 🔗 API Endpoints

```bash
# Товары
GET    /api/products           # Поиск товаров
GET    /api/products/all       # Все товары
POST   /api/products           # Добавить товар
PUT    /api/products/{id}      # Обновить товар
DELETE /api/products/{id}      # Удалить товар

# Продажи
GET    /api/sales              # Все продажи (с фильтром)
POST   /api/sales              # Создать продажу
PUT    /api/sales/{id}/status  # Изменить статус оплаты
DELETE /api/sales/{id}         # Удалить продажу

# История
GET    /api/stock-history      # История операций (с фильтрами)
```

## 🐳 Docker команды

```bash
# Запустить
docker-compose up -d

# Остановить (сохранить БД)
docker-compose stop

# Удалить контейнеры (сохранить БД)
docker-compose down

# Удалить всё включая БД
docker-compose down -v

# Просмотр логов
docker-compose logs -f backend    # Логи бэкенда
docker-compose logs -f frontend   # Логи фронтенда
docker-compose logs -f db         # Логи БД

# Перестроить образы
docker-compose build --no-cache

# Рестарт сервиса
docker-compose restart backend
```

## 📚 Стек технологий

### Backend
- **FastAPI** - Python веб-фреймворк
- **SQLAlchemy** - ORM
- **PostgreSQL** - база данных
- **Pydantic** - валидация данных
- **Uvicorn** - ASGI сервер

### Frontend
- **React 18** - UI библиотека
- **Axios** - HTTP клиент
- **CSS3** - стилизация

### DevOps
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация
- **PostgreSQL 15** - база данных

## 🎨 Варианты развёртывания

### Для разработки (локально)
```bash
# Backend
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload

# Frontend (в отдельном терминале)
cd frontend
npm install
npm start
```

### Для тестирования (Docker)
```bash
docker-compose up -d
# http://localhost:3000
```

### Для production
Раскомментируйте Nginx секцию в `docker-compose.yml`
```bash
docker-compose up -d
# http://localhost (порт 80)
```

## 🔧 Конфигурация

### .env файл
```env
# Database
DB_USER=inventory_user
DB_PASSWORD=inventory_pass
DB_NAME=inventory_db

# Frontend
REACT_APP_API_URL=http://localhost:8000/api
```

## 📝 Примеры API запросов

### Добавить товар со скидкой (-10%)
```bash
curl -X POST http://localhost:8000/api/products \
  -H "Content-Type: application/json" \
  -d '{
    "name": "ТОНЕР ЧЁРНЫЙ C60",
    "sku": "C60-BLK",
    "purchase_price": "3000.00",
    "coefficient": "0.9",
    "quantity": 10
  }'
```

### Поиск товаров
```bash
curl "http://localhost:8000/api/products?q=ТОНЕР"
```

### Создать продажу
```bash
curl -X POST http://localhost:8000/api/sales \
  -H "Content-Type: application/json" \
  -d '{
    "client_name": "ООО Тест",
    "items": [
      {
        "product_id": 1,
        "quantity": 2,
        "sold_price_per_unit": "3800.00",
        "coefficient": "1.0"
      }
    ]
  }'
```

### Получить историю за 7 дней
```bash
curl "http://localhost:8000/api/stock-history?days=7"
```

## 📊 Коэффициенты

### На складе (при закупке)
- **0.9** = скидка 10%
- **1.0** = без коэффициента (стандарт)
- **1.18** = НДС 18%
- **1.2** = наценка 20%

### При продаже (независимо)
- Каждой позиции свой коэффициент
- Не влияет на коэффициент товара
- Используется только для расчёта этой продажи

## 🐛 Решение проблем

### Ошибка подключения к БД
```bash
# Проверьте статус контейнеров
docker-compose ps

# Перезапустите контейнеры
docker-compose restart
```

### Фронтенд не загружается
```bash
# Очистите npm кеш
docker-compose build --no-cache frontend
docker-compose restart frontend
```

### Потеря данных
```bash
# Используйте stop вместо down -v
docker-compose stop      # Сохраняет БД
docker-compose down -v   # УДАЛЯЕТ БД!
```

## 📞 Интерактивная документация

Откройте **http://localhost:8000/docs** - полная документация Swagger UI с возможностью тестирования.

## ✅ Что включено

- ✅ Полнофункциональный фронтенд React
- ✅ API с FastAPI
- ✅ PostgreSQL база данных
- ✅ Docker контейнеризация
- ✅ История всех операций
- ✅ Поддержка коэффициентов (НДС, скидки)
- ✅ Фильтрация и поиск
- ✅ Интерактивная Swagger документация
- ✅ Готово к production

## 📄 Дополнительная документация

- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура системы
- [CHANGES.md](CHANGES.md) - Список изменений
- [README_FULL.md](README_FULL.md) - Полная документация API

---

**Готово к использованию!** 🎉

Вопросы? Откройте issue в репозитории.

- ✅ История всех операций

### 💰 Продажи
- ✅ Создание продаж
- ✅ Коэффициент для каждого товара
- ✅ Расчёт прибыли и себестоимости
- ✅ Управление статусами платежей

### 📊 История
- ✅ Полное отслеживание движения товаров
- ✅ Фильтрация по датам и товарам
- ✅ История всех изменений

---

## 🚀 Быстрый старт

### Через Docker (рекомендуется)
```bash
# 1. Запустить контейнеры
docker-compose up -d

# 2. Открыть документацию
open http://localhost:8000/docs

# 3. Сделать первый запрос (в том же окне Swagger UI)
```

### Локально (без Docker)
```bash
# Требуется: Python 3.11+, PostgreSQL

# 1. Установить зависимости
pip install -r requirements.txt

# 2. Запустить
uvicorn app.main:app --reload
```

Подробная инструкция: [DEPLOYMENT.md](DEPLOYMENT.md)

---

## 📝 Основные операции

### Добавить товар
```bash
curl -X POST http://localhost:8000/api/products \
  -H "Content-Type: application/json" \
  -d '{
    "name": "ТОНЕР ЧЁРНЫЙ",
    "sku": "BLK-001",
    "purchase_price": "3000.00",
    "coefficient": "1.2",
    "quantity": 10
  }'
```

### Поиск товаров
```bash
curl "http://localhost:8000/api/products?q=ТОНЕР"
```

### Создать продажу
```bash
curl -X POST http://localhost:8000/api/sales \
  -H "Content-Type: application/json" \
  -d '{
    "client_name": "ООО Тест",
    "items": [
      {
        "product_id": 1,
        "quantity": 2,
        "sold_price_per_unit": "3800.00",
        "coefficient": "1.0"
      }
    ]
  }'
```

### История операций
```bash
curl "http://localhost:8000/api/stock-history?days=7"
```

Полная справка: [README_FULL.md](README_FULL.md)

---

## 🧪 Тестирование

```bash
# Полный тест API
python test_api_full.py

# Или интерактивно в браузере
http://localhost:8000/docs
```

---

## 📂 Структура проекта

```
inventory_api/
├── app/
│   ├── models/           # Модели БД (Product, Sale, StockOperation)
│   ├── schemas/          # Pydantic схемы для API
│   ├── routes/           # API endpoints
│   ├── services/         # Бизнес логика
│   ├── database.py       # Подключение PostgreSQL
│   └── main.py           # FastAPI приложение
├── docker-compose.yml    # PostgreSQL + App
├── Dockerfile            # Docker образ
├── requirements.txt      # Python зависимости
├── test_api_full.py      # Полный тест
└── DOCS/
    ├── INDEX.md          # Индекс всей документации
    ├── QUICKSTART.md     # За 5 минут
    ├── SUMMARY.md        # Краткая сводка
    ├── FINAL_REPORT.md   # Полный отчёт
    ├── README_FULL.md    # Справка API
    ├── ARCHITECTURE.md   # Архитектура
    ├── CHANGES.md        # Что изменилось
    └── DEPLOYMENT.md     # Как запустить
```

---

## 🔌 API Endpoints

| Метод | Путь | Описание |
|-------|------|---------|
| GET | `/api/products` | Поиск товаров |
| GET | `/api/products/all` | Все товары |
| POST | `/api/products` | Добавить товар |
| PUT | `/api/products/{id}` | Обновить товар |
| DELETE | `/api/products/{id}` | Удалить товар |
| POST | `/api/sales` | Создать продажу |
| GET | `/api/sales` | Все продажи |
| PUT | `/api/sales/{id}/status` | Изменить статус |
| DELETE | `/api/sales/{id}` | Удалить продажу |
| GET | `/api/stock-history` | История операций |

Полная справка: [README_FULL.md](README_FULL.md)

---

## 🛠️ Технологический стек

- **Backend**: FastAPI 0.115
- **ORM**: SQLAlchemy 2.0
- **БД**: PostgreSQL 15
- **Валидация**: Pydantic 2.9
- **Контейнеризация**: Docker & Docker Compose
- **Асинхронность**: Uvicorn
- **Python**: 3.11

---

## 📊 Ключевые особенности

### Коэффициенты
- **На складе** = скидки и НДС от поставщика
- **При продаже** = каждому товару свой (НДС, комиссия, скидка клиенту)

### История
- Все операции логируются (приход, продажа, корректировка)
- Хранится старое и новое значение
- Сохраняется дата и причина

### Продажи
- Один товар может быть в продаже несколько раз
- Каждая позиция имеет свой коэффициент
- Автоматический расчёт сумм и прибыли

---

## ✅ Требования выполнены

- ✅ Интерактивный поиск товаров
- ✅ Добавление товаров с коэффициентом
- ✅ Редактирование товаров
- ✅ История по датам
- ✅ Создание продаж
- ✅ Коэффициент при продаже
- ✅ Расчёты сумм и маржи
- ✅ Управление статусами платежей
- ✅ Полная документация
- ✅ Docker для запуска

---

## 🆘 Помощь

### Я хочу запустить приложение
→ [QUICKSTART.md](QUICKSTART.md)

### Я не знаю как использовать API
→ [README_FULL.md](README_FULL.md) или [http://localhost:8000/docs](http://localhost:8000/docs)

### Я хочу понять архитектуру
→ [ARCHITECTURE.md](ARCHITECTURE.md)

### Я хочу всё скачать и почитать
→ [INDEX.md](INDEX.md) — полный индекс всей документации

---

## 📞 Версия и дата

**Версия**: 1.1 (с поддержкой коэффициентов)  
**Дата**: 27 декабря 2025 г.  
**Статус**: Готово к использованию ✅

---

**Начни с [QUICKSTART.md](QUICKSTART.md) — запустишь за 5 минут! 🚀**
